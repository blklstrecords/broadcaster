<icecast>
  <location>Helsinki, Finland</location>
  <admin>radio@blklst.fi</admin>

  <!-- TÄMÄ NÄKYY KUUNTELIJOILLE -->
  <hostname>radio.blklst.fi</hostname>

  <limits>
    <clients>200</clients>
    <sources>5</sources>
    <queue-size>524288</queue-size>
    <client-timeout>30</client-timeout>
    <header-timeout>15</header-timeout>
    <source-timeout>10</source-timeout>
    <burst-on-connect>1</burst-on-connect>
    <burst-size>65535</burst-size>
  </limits>

  <authentication>
    <!-- RPi käyttää tätä: user=source pass=xxxxxx -->
    <source-password>xxxxxx</source-password>
    <relay-password>xxxxxx</relay-password>

    <admin-user>admin</admin-user>
    <admin-password>xxxxxx</admin-password>
  </authentication>

  <listen-socket>
    <port>8000</port>
  </listen-socket>

  <!-- CORS jotta player/status toimii embedissä -->
  <http-headers>
    <header name="Access-Control-Allow-Origin" value="*" />
    <header name="Access-Control-Allow-Methods" value="GET, OPTIONS" />
  </http-headers>

  <fileserve>1</fileserve>

  <!-- Mount jossa stream tulee sisään ja tallennetaan -->
  <mount type="normal">
    <mount-name>/live.mp3</mount-name>

    <!-- SESSION-TALLENNUS: yksi tiedosto per source-yhteys -->
    <!-- HUOM: ei ":" koska Windows -->
    <dump-file>D:\Recordings\current.mp3</dump-file>

    <public>0</public>
    <max-listeners>200</max-listeners>
  </mount>

  <paths>
    <!-- NÄMÄ TÄRKEITÄ WINDOWSISSA -->
    <basedir>C:\Program Files (x86)\Icecast</basedir>
    <logdir>C:\Program Files (x86)\Icecast\log</logdir>
    <webroot>C:\Program Files (x86)\Icecast\web</webroot>
    <adminroot>C:\Program Files (x86)\Icecast\admin</adminroot>

    <alias source="/" destination="/status.xsl" />
  </paths>

  <logging>
    <accesslog>access.log</accesslog>
    <errorlog>error.log</errorlog>
    <loglevel>3</loglevel>
    <logsize>10000</logsize>
  </logging>
</icecast>
